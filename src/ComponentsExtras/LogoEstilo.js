import React from 'react';
import { StyleSheet } from 'react-native';

export const ConfigLogo = StyleSheet.create({
    Logo: {
        width: 70,
        height: 100,
        resizeMode: "contain",
    },
    Titulo: {
        fontFamily: "Roboto-Bold",
        color: "#268596",
        fontSize: 28,
        marginBottom: 20
    },
});